syntax = "proto3";

package db_server;

message rsp_msg
{
    int32 code = 1;
    string msg = 2;
}

message req_login
{
    string  userid=1;
    string  password=2;
}

message rsp_login
{
    rsp_msg msg = 1;
}